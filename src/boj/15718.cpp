#include <iostream>

using namespace std;

int t, n, m;

int main() {
    cin.tie(0);
    ios_base::sync_with_stdio(0);

    cin >> t;
    while(t--) {
        cin >> n >> m;
        
    }

    return 0;
}
