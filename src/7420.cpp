#include <iostream>

#define N 1000

using namespace std;

typedef long long ll;

struct pt { ll x, y; };

int n, l;
pt a[N];

int main()
{
    cin.tie(0); cout.tie(0);
    ios_base::sync_with_stdio(0);
    
    return 0;
}