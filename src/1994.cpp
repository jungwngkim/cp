#include <iostream>
#include <vector>

using namespace std;

struct el {
    int last, diff, len;
};

int n, a[2000];
vector<el> v;

int main()
{
    cin.tie(0);  
    ios_base::sync_with_stdio(0);

    cin >> n;
    for(int i = 0; i < n; i++) cin >> a[i];

    for(int i = 0; i < n; i++)
    {
        
    }
    
    return 0;
}